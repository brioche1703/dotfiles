selectp -t 0 # select the first (0) pane
split-window -h -p 70 -c '#{pane_current_path}' # split it into two halves

selectp -t 1 # select the new, second (1) pane
split-window -v -c '#{pane_current_path}' # split it into two halves
selectp -t 0 # go back to the first pane
